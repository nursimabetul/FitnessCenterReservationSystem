<nav class="app-header navbar navbar-expand bg-body">
    <div class="container-fluid">

        <!-- Sidebar Toggle Button -->
        <button class="btn btn-link nav-link" data-lte-toggle="sidebar" type="button" aria-label="Toggle Sidebar">
            <i class="bi bi-list" style="font-size: 1.3rem;"></i>
        </button>

        <!-- Left Navbar Links -->
        <ul class="navbar-nav ms-2">
            <li class="nav-item d-none d-md-block">
                <a href="/" class="nav-link">Anasayfa</a>
            </li>
        </ul>

        <!-- Right Side of Navbar -->
        <ul class="navbar-nav ms-auto">

            <!-- Search Icon -->
            <li class="nav-item dropdown">
                <a class="nav-link" data-bs-toggle="dropdown" href="#" aria-expanded="false">
                    <i class="bi bi-search"></i>
                </a>
                <div class="dropdown-menu dropdown-menu-end p-3">
                    <form class="d-flex">
                        <input class="form-control me-2" type="search" placeholder="Ara..." required>
                        <button class="btn btn-primary" type="submit">Ara</button>
                    </form>
                </div>
            </li>

            <!-- Notifications -->
            <li class="nav-item dropdown">
                <a class="nav-link" data-bs-toggle="dropdown" href="#">
                    <i class="bi bi-bell"></i>
                    <span class="navbar-badge badge text-bg-danger">3</span>
                </a>
                <div class="dropdown-menu dropdown-menu-end p-0">
                    <div class="card card-body">
                        <h6 class="dropdown-header">Bildirimler</h6>
                        <a href="#" class="dropdown-item">
                            <i class="bi bi-calendar-event me-2"></i> Yeni randevu kaydı
                        </a>
                        <a href="#" class="dropdown-item">
                            <i class="bi bi-person-plus me-2"></i> Yeni üye kaydı
                        </a>
                        <a href="#" class="dropdown-item">
                            <i class="bi bi-info-circle me-2"></i> Sistem duyurusu
                        </a>
                    </div>
                </div>
            </li>

            <!-- User Menu -->
            <li class="nav-item dropdown user-menu">
                <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown">
                    <img src="https://ui-avatars.com/api/?name=Admin" class="user-image rounded-circle" alt="User Image">
                    <span class="d-none d-md-inline">Admin</span>
                </a>
                <ul class="dropdown-menu dropdown-menu-end">
                    <!-- User image -->
                    <li class="user-header text-center p-3">
                        <img src="https://ui-avatars.com/api/?name=Admin" class="rounded-circle mb-2" alt="User Image" width="80">
                        <p class="mb-0">
                            Admin Paneli<br />
                            <small>Fitness Center Yönetimi</small>
                        </p>
                    </li>

                    <!-- Menu Body -->
                    <li>
                        <a href="/Account/Profile" class="dropdown-item">
                            <i class="bi bi-person-circle me-2"></i> Profilim
                        </a>
                    </li>

                    <!-- Logout -->
                    <li>
                        <form method="post" action="/Account/Logout">
                            <button class="dropdown-item text-danger">
                                <i class="bi bi-box-arrow-right me-2"></i> Çıkış Yap
                            </button>
                        </form>
                    </li>
                </ul>
            </li>

        </ul>
    </div>
</nav>

@model IEnumerable<dynamic>
@{
    ViewData["Title"] = "Pasif / Reddedilen Kullanıcılar";
}

<h4 class="mb-3">
    <i class="bi bi-person-x-fill text-secondary me-1"></i>
    @ViewData["Title"]
</h4>

@if (!Model.Any())
{
    <div class="alert alert-info">Hiç pasif veya reddedilen kullanıcı yok.</div>
}
else
{
    @* Index.cshtml ile aynı tabloyu kullanabiliriz *@
    @await Html.PartialAsync("KullaniciTabloPartial", Model)
}
